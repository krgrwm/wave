vをすべて1.0に．PlaneWaveの誤差はそのままでいい
エネルギーの計算をする．なんかうまく行っていない．perlで計算したものとも合わない
    gauss
    解放端だと速度の二乗が増加していく
    固定端だとエネルギーが大体一定．精度は悪い
    vの二乗が振動っぽい感じ
    ポテンシャルはほぼ一定

set/gauss.setのプロットが時間とともにz軸方向に平行移動していってる．なぜ?
